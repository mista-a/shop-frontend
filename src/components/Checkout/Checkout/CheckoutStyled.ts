import { styled } from '@mui/material'

export const StyledCheckout = styled('div')({
  display: 'grid',
  gridTemplateColumns: 'auto minmax(500px, 40%)',
  justifyContent: 'space-between',
  width: '1300px',
  margin: '0 auto',
})
