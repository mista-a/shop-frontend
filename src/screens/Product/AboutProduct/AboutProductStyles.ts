import { styled } from '@mui/material'

export const StyledSectionNames = styled('div')({
  display: 'flex',
  justifyContent: 'space-between',
  maxWidth: 235,
})

export const StyledActiveSection = styled('div')({
  color: '#FF6565',
})

export const StyledProductHeader = styled('div')({
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
})
